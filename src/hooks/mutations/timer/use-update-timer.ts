// import { useMutation } from "@tanstack/react-query";
// import { updateTimer } from "../../../api/timer";
// import type { SplitTime } from "../../../types";

// export function useUpdateTimer(timerId: string, splitTimes: SplitTime[]) {
//   return useMutation({
//     mutationFn: () => updateTimer(timerId, splitTimes),
//     onSuccess: () => {},
//   });
// }
